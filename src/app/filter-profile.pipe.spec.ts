import { FilterProfilePipe } from './filter-profile.pipe';

describe('FilterProfilePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterProfilePipe();
    expect(pipe).toBeTruthy();
  });
});
